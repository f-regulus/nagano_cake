<h2>マイページ</h2>
<h4>登録情報</h4>

<%= link_to "編集する", customers_mypage_edit_path %>


<table>
  <tbody>
    <% @customer.each do |customer| %> 
    <tr>
      <td>氏名</td>
      <td><%= customer.first_name %></td>
    </tr>
      <td>カナ</td>
      <td><%= customer.kana_first_name %></td>
    </tr>
      <td>郵便番号</td>
      <td><%= customer.postal_code %></td>
    </tr>
    </tr>
      <td>住所</td>
      <td><%= customer.address %></td>
    </tr>
    </tr>
      <td>電話番号</td>
      <td><%= customer.telephone_number %></td>
    </tr>
    </tr>
      <td>メールアドレス</td>
      <td><%= customer.email %></td>
    </tr>
    <% end %>
  </tbody>
</table>

配送先
<%= link_to "一覧を見る",deliveries_path %>

注文履歴
<%= link_to "一覧", customers_mypage_edit_path %>